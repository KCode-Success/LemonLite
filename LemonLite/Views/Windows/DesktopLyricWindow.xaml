<Window x:Class="LemonLite.Views.Windows.DesktopLyricWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:behavior="clr-namespace:LemonLite.Behaviors"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:loc="clr-namespace:LemonLite.Services"
        xmlns:local="clr-namespace:LemonLite.Views.Windows"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ut="clr-namespace:LemonLite.Utils"
        xmlns:vm="clr-namespace:LemonLite.ViewModels"
        xmlns:ws="https://schemas.elecho.dev/wpfsuite"
        x:Name="window"
        Title="DesktopLyricWindow"
        Width="720"
        Height="145"
        MinHeight="32"
        d:DataContext="{d:DesignInstance Type=vm:DesktopLyricWindowViewModel}"
        AllowsTransparency="True"
        Background="{x:Null}"
        FontFamily="{StaticResource DefaultFontFamily}"
        Foreground="{DynamicResource ForeColor}"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        WindowStyle="None"
        mc:Ignorable="d">
    <i:Interaction.Behaviors>
        <behavior:WindowDragMoveBehavior />
    </i:Interaction.Behaviors>
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" GlassFrameThickness="-1" />
    </WindowChrome.WindowChrome>
    <Window.Resources>
        <ws:BindingProxy x:Key="proxy" Data="{Binding}" />
    </Window.Resources>
    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem Command="{Binding Data.FontSizeUpCommand, Source={StaticResource proxy}}"
                      Header="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[FontSizeUp], Mode=OneWay}"
                      Icon="{x:Null}" />
            <MenuItem Command="{Binding Data.FontSizeDownCommand, Source={StaticResource proxy}}"
                      Header="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[FontSizeDown], Mode=OneWay}"
                      Icon="{x:Null}" />
            <MenuItem Command="{Binding Data.ShowMainWindowCommand, Source={StaticResource proxy}}"
                      Header="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[ShowMainWindow], Mode=OneWay}"
                      Icon="{x:Null}" />
            <Separator Margin="0,4,24,4" Opacity="0.2" />
            <MenuItem Command="{Binding Data.PlayLastCommand, Source={StaticResource proxy}}"
                      Header="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[PlayTheLastOne], Mode=OneWay}"
                      Icon="{x:Null}" />
            <MenuItem Command="{Binding Data.PlayOrPauseCommand, Source={StaticResource proxy}}"
                      Header="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[PlayOrPause], Mode=OneWay}"
                      Icon="{x:Null}" />
            <MenuItem Command="{Binding Data.PlayNextCommand, Source={StaticResource proxy}}"
                      Header="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[PlayTheNextOne], Mode=OneWay}"
                      Icon="{x:Null}" />

        </ContextMenu>
    </Window.ContextMenu>
    <ws:BoxPanel x:Name="windowRoot" Background="#01000000">
        <ut:TopCutBorder x:Name="IsLandBaseBackground"
                         Background="{DynamicResource IslandBackgroundColor}"
                         CornerRadius="24"
                         TopCutRadius="8"
                         Visibility="Collapsed" />
        <ut:TopCutBorder x:Name="AnimatedBackgroundBd"
                         CornerRadius="12"
                         Visibility="{Binding BackgroundVisibility}">
            <ut:TopCutBorder.Background>
                <ImageBrush ImageSource="{Binding BackgroundImageSource, Mode=OneWay}" Stretch="UniformToFill">
                    <ImageBrush.RelativeTransform>
                        <TransformGroup>
                            <TranslateTransform Y="0.5" />
                            <ScaleTransform CenterX="0.5" CenterY="1" ScaleX="6" ScaleY="2.4" />
                            <RotateTransform Angle="0" CenterX="0.5" CenterY="1">
                                <i:Interaction.Behaviors>
                                    <behavior:RotateWithPlayingStateBehavior IsEnabled="{Binding IsBackgroundValid}" IsPlaying="{Binding IsPlaying}" />
                                </i:Interaction.Behaviors>
                            </RotateTransform>
                        </TransformGroup>
                    </ImageBrush.RelativeTransform>
                </ImageBrush>
            </ut:TopCutBorder.Background>
            <i:Interaction.Behaviors>
                <behavior:FadeBorderBehavior Source="{Binding BackgroundImageSource}" Duration="00:00:1.5" />
            </i:Interaction.Behaviors>
        </ut:TopCutBorder>
        <Grid x:Name="LrcPanel" IsHitTestVisible="False">
            <ScrollViewer x:Name="LrcScrollViewer"
                          Grid.Column="1"
                          Margin="24,8"
                          HorizontalScrollBarVisibility="Hidden"
                          PanningMode="HorizontalOnly"
                          VerticalScrollBarVisibility="Disabled">
                <ContentControl x:Name="LrcHost"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Content="{Binding LyricControl}" />
            </ScrollViewer>
        </Grid>

        <!--  功能区 鼠标进入后显示  -->
        <Grid x:Name="FuncPanel"
              d:IsHidden="True"
              d:Visibility="Visible"
              Visibility="Collapsed">
            <Border Background="{DynamicResource WindowBackgroundColor}"
                    BorderBrush="{DynamicResource BorderColor}"
                    BorderThickness="1"
                    CornerRadius="12" />
            <ws:WrapPanel HorizontalAlignment="Center"
                          VerticalAlignment="Center"
                          HorizontalSpacing="24">
                <ToggleButton Width="48"
                              Height="48"
                              Padding="12"
                              IsChecked="{Binding ShowTranslation}"
                              Style="{StaticResource IconToggleButton}"
                              Tag="{StaticResource Icon_Trans}"
                              ToolTip="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[ShowTranslation], Mode=OneWay}" />
                <ws:Button x:Name="PlayLastBtn"
                           Width="48"
                           Height="48"
                           HorizontalAlignment="Left"
                           Command="{Binding PlayLastCommand}"
                           Style="{StaticResource SimpleIconButtonStyleForWs}"
                           ToolTip="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[PlayTheLastOne], Mode=OneWay}">
                    <Path Height="22"
                          Data="{DynamicResource PlayIcon_Last}"
                          Fill="{DynamicResource ForeColor}"
                          Stretch="Uniform" />
                </ws:Button>
                <ws:Button x:Name="PlayBtn"
                           Width="54"
                           Height="54"
                           Command="{Binding PlayOrPauseCommand}"
                           Style="{StaticResource SimpleIconButtonStyleForWs}"
                           ToolTip="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[PlayOrPause], Mode=OneWay}">
                    <ws:ConditionalControl Condition="{Binding IsPlaying}">
                        <ws:ConditionalControl.ContentWhenFalse>
                            <Path Data="{DynamicResource PlayIcon_Play}"
                                  Fill="{DynamicResource HighlightThemeColor}"
                                  Stretch="Uniform" />
                        </ws:ConditionalControl.ContentWhenFalse>
                        <ws:ConditionalControl.ContentWhenTrue>
                            <Path Data="{DynamicResource PlayIcon_Pause}"
                                  Fill="{DynamicResource HighlightThemeColor}"
                                  Stretch="Uniform" />
                        </ws:ConditionalControl.ContentWhenTrue>
                    </ws:ConditionalControl>
                </ws:Button>
                <ws:Button x:Name="PlayNextBtn"
                           Width="48"
                           Height="48"
                           HorizontalAlignment="Right"
                           Command="{Binding PlayNextCommand}"
                           Style="{StaticResource SimpleIconButtonStyleForWs}"
                           ToolTip="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[PlayTheNextOne], Mode=OneWay}">
                    <Path Height="22"
                          Data="{DynamicResource PlayIcon_Next}"
                          Fill="{DynamicResource ForeColor}"
                          Stretch="Uniform" />
                </ws:Button>
                <ToggleButton Width="48"
                              Height="48"
                              Padding="12"
                              IsChecked="{Binding ShowRomaji}"
                              Style="{StaticResource IconToggleButton}"
                              Tag="{StaticResource Icon_Romaji}"
                              ToolTip="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[ShowRomaji], Mode=OneWay}" />
            </ws:WrapPanel>
            <ws:Button x:Name="CloseBtn"
                       Width="32"
                       Height="32"
                       Margin="8"
                       HorizontalAlignment="Right"
                       VerticalAlignment="Top"
                       Click="CloseBtn_Click"
                       Content="&#xE8BB;"
                       FontFamily="{StaticResource SegoeFluentIcon}"
                       FontSize="10"
                       Style="{StaticResource SimpleIconButtonStyleForWs}"
                       ToolTip="{Binding Source={x:Static loc:LocalizationService.Instance}, Path=[CloseDesktopLyricViewer], Mode=OneWay}" />
        </Grid>

        <!--  interactive info on Island Mode  -->
        <Grid x:Name="InfoPanel"
              Height="146"
              Margin="20,0"
              Visibility="Collapsed">
            <Grid HorizontalAlignment="Center">
                <Border Width="64"
                        Height="64"
                        Margin="18,24,0,0"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Top"
                        d:Background="Pink"
                        Background="{Binding CoverImage}"
                        CornerRadius="12"
                        RenderOptions.BitmapScalingMode="HighQuality" />
                <TextBlock x:Name="TitleTb"
                           Margin="96,32,20,0"
                           VerticalAlignment="Top"
                           d:Text="Hello"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Text="{Binding SongTitle}"
                           TextTrimming="CharacterEllipsis" />
                <TextBlock x:Name="ArtistTb"
                           Margin="96,56,20,0"
                           VerticalAlignment="Top"
                           d:Text="hello hello"
                           FontSize="12"
                           Foreground="{DynamicResource HintForeColor}"
                           TextTrimming="CharacterEllipsis">
                    <TextBlock.Text>
                        <MultiBinding StringFormat="{}{0} · {1}">
                            <Binding Mode="OneWay" Path="ArtistName" />
                            <Binding Mode="OneWay" Path="AlbumName" />
                        </MultiBinding>
                    </TextBlock.Text>
                </TextBlock>
            </Grid>
            <ws:FlexPanel MaxWidth="240"
                          Margin="0,0,0,12"
                          VerticalAlignment="Bottom"
                          MainAlignment="SpaceEvenly">
                <ws:Button Width="36"
                           Height="36"
                           Margin="0,3,0,3"
                           HorizontalAlignment="Left"
                           Command="{Binding PlayLastCommand}"
                           FontFamily="{StaticResource SegoeFluentIcon}"
                           FontSize="18"
                           Style="{StaticResource SimpleIconButtonStyleForWs}"
                           ToolTip="{Binding [PlayTheLastOne], Mode=OneWay, Source={x:Static loc:LocalizationService.Instance}}">
                    &#xf8ac;
                </ws:Button>
                <ws:Button Width="36"
                           Height="36"
                           Command="{Binding PlayOrPauseCommand}"
                           FontFamily="{StaticResource SegoeFluentIcon}"
                           FontSize="20"
                           Style="{StaticResource SimpleIconButtonStyleForWs}"
                           ToolTip="{Binding [PlayOrPause], Mode=OneWay, Source={x:Static loc:LocalizationService.Instance}}">
                    <ws:ConditionalControl Condition="{Binding IsPlaying}">
                        <ws:ConditionalControl.ContentWhenFalse>
                            &#xf5b0;
                        </ws:ConditionalControl.ContentWhenFalse>
                        &#xf8ae;
                    </ws:ConditionalControl>
                </ws:Button>
                <ws:Button Width="36"
                           Height="36"
                           HorizontalAlignment="Left"
                           Command="{Binding PlayNextCommand}"
                           FontFamily="{StaticResource SegoeFluentIcon}"
                           FontSize="18"
                           Style="{StaticResource SimpleIconButtonStyleForWs}"
                           ToolTip="{Binding [PlayTheNextOne], Mode=OneWay, Source={x:Static loc:LocalizationService.Instance}}">
                    &#xf8ad;
                </ws:Button>
            </ws:FlexPanel>
        </Grid>
    </ws:BoxPanel>
</Window>
